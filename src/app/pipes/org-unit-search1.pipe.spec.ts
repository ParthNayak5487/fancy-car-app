import { OrgUnitSearchPipe } from './org-unit-search.pipe';

describe('OrgUnitSearch1Pipe', () => {
  it('create an instance', () => {
    const pipe = new OrgUnitSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
